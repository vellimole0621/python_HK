# for
fruits = ["Apple", "Peach", "Pear"]
for fruit in fruits:
    print(fruit)

# sum() 함수 // 값을 전부 합쳐준다.
# min() 함수 // 리스트 내에 가장 작은 값을 표시한다.

# Range
"""
for number in range(a, b, c): # a~(b-1)까지 c 단위로 변화 (c 안 쓰면 1씩 증가)
    print(number)
"""

# choice
# random.choice(~) 하면 ~ 리스트 안의 값 중에서 랜덤으로 고른다.
# random.shuffle(~) 하면 ~ 리스트 안의 값을 섞는다.